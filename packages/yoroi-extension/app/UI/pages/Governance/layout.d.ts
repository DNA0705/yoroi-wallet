import React from 'react';
type Props = {
    stores: any;
    actions: any;
    children: React.ReactNode;
};
declare const GovernanceLayout: ({ stores, actions, children }: Props) => any;
export default GovernanceLayout;
